<h1>SurveyFormV2 Premium — Partner AI Handoff Prompt</h1><hr><h2>CONTEXT</h2><p>You are upgrading an existing React site survey app. Two drop-in replacement files have been provided:</p><ul> <li><code>SurveyFormV2.tsx</code> — 4-step guided survey form component (980 lines)</li> <li><code>SurveyFormV2.css</code> — companion styles (1,384 lines)</li> </ul><p>These files are a <strong>complete, tested, zero-dependency upgrade</strong> to the existing <code>SurveyForm.tsx</code>. The TypeScript build is confirmed clean. Your job is to drop them in and wire them up — nothing more.</p><hr><h2>STACK (do not change)</h2><ul> <li>React 18 + Vite + TypeScript</li> <li>Vanilla CSS only — <strong>no Tailwind, no CSS-in-JS, no new UI libraries</strong></li> <li>All CSS uses the existing project CSS variables (<code>--color-primary</code>, <code>--color-bg</code>, <code>--radius-sm/md/lg</code>, <code>--shadow-sm/md</code>)</li> </ul><hr><h2>✅ DO THIS</h2><p><strong>1. Copy the two files into place</strong></p><pre><code>src/components/SurveyFormV2.tsx
src/components/SurveyFormV2.css
</code></pre><p><strong>2. Update App.tsx — swap the import only</strong></p><pre><code class="language-tsx">// Remove this:
import SurveyForm from './components/SurveyForm';

// Add this:
import SurveyFormV2 from './components/SurveyFormV2';
</code></pre><p><strong>3. Swap the JSX usage in App.tsx</strong></p><pre><code class="language-tsx">// Remove this:
&lt;SurveyForm ... /&gt;

// Replace with (same props):
&lt;SurveyFormV2 ... /&gt;
</code></pre><p><strong>4. Run a TypeScript check to confirm clean build</strong></p><pre><code>npx tsc --noEmit
</code></pre><p><strong>5. Run the dev server and test all 4 steps</strong></p><pre><code>npm run dev
</code></pre><p><strong>6. Verify these work end-to-end:</strong></p><ul> <li>Step 1: Project type chip selection, site details fields, inspector fields</li> <li>Step 2: Checklist cards — tap card to cycle status, chip buttons, notes</li> <li>Step 3: Photo slots — camera capture, thumbnail preview, required slot warning</li> <li>Step 4: Review — Edit buttons jump back to correct step, submit → success screen</li> </ul><hr><h2>❌ DO NOT DO THIS</h2><p><strong>Do NOT change the props interface.</strong> <code>SurveyFormV2</code> accepts the exact same props as the original <code>SurveyForm</code>. Do not add, remove, or rename any props.</p><p><strong>Do NOT change the API calls.</strong> The component already calls <code>createSurvey</code> and <code>updateSurvey</code> from <code>../api/surveyApi</code>. Do not modify these calls, their arguments, or their import path.</p><p><strong>Do NOT change the data types.</strong> <code>Survey</code>, <code>ChecklistItem</code>, <code>Photo</code>, <code>GpsCoordinates</code>, <code>SurveyStatus</code>, <code>ChecklistStatus</code> — all come from <code>../types/survey</code>. Leave them untouched.</p><p><strong>Do NOT install new dependencies.</strong> Zero new packages are required. Everything is pure React hooks + vanilla CSS.</p><p><strong>Do NOT split the CSS into multiple files.</strong> <code>SurveyFormV2.css</code> is self-contained. All <code>sv2-*</code> class names are scoped to this component. Keep it as one file.</p><p><strong>Do NOT rename the CSS classes.</strong> All class names use the <code>sv2-</code> prefix to avoid collisions. Do not rename them or convert them to modules.</p><p><strong>Do NOT change the step count.</strong> The form is intentionally 4 steps. Do not add, remove, or reorder steps.</p><p><strong>Do NOT touch the checklist CYCLE_ORDER logic.</strong> The tap-to-cycle behavior (<code>pending → pass → fail → n/a → pending</code>) is intentional UX. Leave it.</p><p><strong>Do NOT modify the success screen behavior.</strong> After submit, the full-screen success card appears before <code>onSaved()</code> is called. This is intentional — do not skip it or replace it with a toast/alert.</p><p><strong>Do NOT change the photo slot keys.</strong> The <code>PHOTO_SLOTS</code> array defines slot keys that map to the <code>Survey.photos</code> array. Changing keys will break photo persistence.</p><hr><h2>COMMON MISTAKES TO AVOID</h2><table class="e-rte-table"> <thead> <tr> <th>Mistake</th> <th>Why it breaks</th> </tr> </thead> <tbody><tr> <td>Importing from <code>../api/surveys</code></td> <td>File doesn't exist — correct path is <code>../api/surveyApi</code></td> </tr> <tr> <td>Typing <code>buildPayload</code> as <code>Omit&lt;Survey, 'id' | ...&gt;</code></td> <td><code>createSurvey</code> requires the <code>id</code> field — do not omit it</td> </tr> <tr> <td>Adding <code>import React from 'react'</code></td> <td>Not needed with Vite's JSX transform — causes TS6133 warning</td> </tr> <tr> <td>Wrapping V2 in the existing <code>&lt;main&gt;</code> / <code>&lt;Header&gt;</code> layout</td> <td>V2 renders its own full-screen shell — render it directly, not inside the page layout</td> </tr> </tbody></table><hr><h2>VERIFICATION CHECKLIST</h2><p>Before calling this done, confirm all of these:</p><ul> <li><input disabled="" type="checkbox"> <code>npx tsc --noEmit</code> exits with zero errors</li> <li><input disabled="" type="checkbox"> <code>npm run build</code> completes successfully</li> <li><input disabled="" type="checkbox"> Step 1 loads with project type chips and site detail fields</li> <li><input disabled="" type="checkbox"> Step 2 shows checklist with segmented color bar and tap-to-cycle</li> <li><input disabled="" type="checkbox"> Step 3 shows photo grid with amber pulse on required empty slots</li> <li><input disabled="" type="checkbox"> Step 4 shows review with Edit buttons and confidence banner</li> <li><input disabled="" type="checkbox"> Submitting shows full-screen success card with Survey ID</li> <li><input disabled="" type="checkbox"> "Back to Surveys" on success screen returns to survey list</li> <li><input disabled="" type="checkbox"> No console errors in browser dev tools</li> </ul><hr><h2>FILES TO LEAVE UNTOUCHED</h2><pre><code>src/api/surveyApi.ts        ← do not modify
src/types/survey.ts         ← do not modify
src/components/SurveyForm.tsx   ← can be kept as backup, just don't use it
</code></pre><hr><p><em>Drop in the two files, swap the import, run tsc — you're done.</em></p>